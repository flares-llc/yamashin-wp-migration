{
  "name": "yamashin-wp-migration-mcp",
  "version": "1.0.0",
  "description": "stdio bridge for the Yamashin WP Migration Streamable HTTP MCP server",
  "type": "module",
  "bin": {
    "yamashin-wp-migration-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flares-llc/yamashin-wp-migration.git",
    "directory": "mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@types/node": "24.1.0",
    "typescript": "5.8.3"
  },
  "overrides": {
    "@hono/node-server": "2.0.12"
  }
}
